version: ‘3’
services:
  api:
    build: ./backend
    container_name: backend
    working_dir: /app
    ports:
      - 8080:8000
  gui:
    restart: always
    container_name: frontend
    working_dir: /app
    build: ./frontend
    ports:
      - "8001:80"

networks:
  fastapi_net: